{
  "description": "Apply /backlog:* commands before they reach the model",
  "hooks": {
    "UserPromptExpansion": [
      {
        "matcher": "^(backlog:)?(add|list|doing|done|todo|wontfix|rm)$",
        "hooks": [
          {
            "type": "command",
            "command": "python3",
            "args": [
              "${CLAUDE_PLUGIN_ROOT}/hooks/backlog_hook.py",
              "--project",
              "${CLAUDE_PROJECT_DIR}"
            ],
            "timeout": 10
          }
        ]
      }
    ]
  }
}
